<nav class="navColor navbar navbar-expand-lg w-100 p-3">
  <section class="container-fluid">
    <a class="navbar-brand" href="#">
      <img src="/static/images/Untitled (3).png" alt="Logo" width="150" class="d-inline-block align-text-top">
    </a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/map">Maps </a>
        </li>
        <li class="nav-item">
          <a class="nav-link"  href="/restaurant_browser">Browser</a>
        </li>
      </ul>

      
      {# Authentication #}
      <article style="float: right;">
        {% if current_user.is_authenticated %}
          <div class="btn-group">
            <button type="button" class="btn btn-secondary dropdown-toggle" data-bs-toggle="dropdown"
              data-bs-display="static" aria-expanded="false">
              {{ current_user.first_name + current_user.last_name }}
            </button>
            <ul class="dropdown-menu dropdown-menu-lg-end">
              <li><a class="dropdown-item" href="/sign_out">Sign Out</a></li>
            </ul>
          </div>
        {% else %}
          <div class="btn-group">
            <button type="button" class="btn btn-light dropdown-toggle" data-bs-toggle="dropdown"
              data-bs-display="static" aria-expanded="false">
              Account
            </button>
            <ul class="dropdown-menu dropdown-menu-lg-end">
              <li>
                <a class="dropdown-item" href="/sign_in">
                  Sign In
                </a>
              </li>
              <li>
                <a class="dropdown-item" href="/sign_up">
                  Sign Up
                </a>
              </li>
            </ul>
          </div>
        {% endif %}
      </article>
    </div>
  </section>
</nav>